// Shipping form data structure
export interface FormData {
  origin: string;
  destination: string;
  cargoType: string;
  weight: string;
  shipDate: string;
  company: string;
  contact: string;
  email: string;
  phone: string;
  priority: string;
}